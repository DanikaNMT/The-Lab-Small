export interface Product {
    id: number
    imgUrl: string
    name: string
    price: number
    amount: number
}

export interface ProductData {
    imgUrl: string
    name: string
    price: number
    amount: number
}